apiVersion: v1
kind: Service
metadata:
  name: {{ template "springboot-multiple-module.fullname" . }}
  labels:
    app: {{ template "springboot-multiple-module.name" . }}
    chart: {{ template "springboot-multiple-module.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "springboot-multiple-module.name" . }}
    release: {{ .Release.Name }}
